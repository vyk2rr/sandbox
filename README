

   install dependencies:
     $ cd landingpage && npm install

   build for first:
     $ grunt

   run the app:
     $ DEBUG=landingpage:* npm start

   look for file changes to refresh:
     $ grunt watch

